/** System group slug: holds all users who have admin rights to any group (managed automatically, not editable). */
export const GROUPADMIN_GROUP_SLUG = 'groupadmin'

/** System group slug: system administrators. */
export const ADMIN_GROUP_SLUG = 'admin'
